<div class="min-h-screen bg-gray-50 flex items-center justify-center px-4">
  <div class="bg-white rounded-lg shadow-lg max-w-md w-full overflow-hidden">
    <!-- Material Design Header -->
    <div class="bg-green-600 px-6 py-8 text-center">
      <div class="w-16 h-16 bg-white/20 rounded-full flex items-center justify-center mx-auto mb-4">
        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
        </svg>
      </div>
      <h1 class="text-2xl font-medium text-white mb-2">ルームに参加</h1>
      <p class="text-green-100">参加者名を入力してルームに参加します</p>
    </div>
    
    <!-- Form Content -->
    <div class="p-6">
      <%= form_with url: join_room_path(@room.id), method: :post, local: true, class: 'space-y-6' do %>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">参加者名</label>
          <%= text_field_tag :name, '', 
              placeholder: 'あなたの名前を入力',
              class: 'w-full px-3 py-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-green-500 transition-colors',
              required: true %>
        </div>
        
        <%= submit_tag 'ルームに参加', 
            class: 'w-full bg-green-600 hover:bg-green-700 text-white font-medium py-3 px-4 rounded shadow-sm transition-colors duration-200' %>
      <% end %>
      
      <div class="mt-6 pt-6 border-t border-gray-200">
        <p class="text-xs text-gray-500 text-center">
          参加後、オーナーが抽選を開始するまでお待ちください
        </p>
      </div>
    </div>
  </div>
</div>
